import React, { Component } from "react";
import Loading from "../SongLoading"

class AuthCallback extends Component {
  render() {
    return (
      <Loading />
    );
  }
}

export default AuthCallback;
